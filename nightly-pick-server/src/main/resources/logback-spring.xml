<configuration>
    <springProperty scope="context" name="CONVERSATION_LOG_PATH" source="server.conversation-log-path" defaultValue="/app/logs/conversation-events.jsonl"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} [requestId=%X{requestId} sessionId=%X{sessionId} traceId=%X{traceId}] - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="CONVERSATION_FILE" class="ch.qos.logback.core.FileAppender">
        <file>${CONVERSATION_LOG_PATH}</file>
        <append>true</append>
        <encoder>
            <pattern>%msg%n</pattern>
        </encoder>
    </appender>

    <logger name="com.nightlypick.server.conversation.event" level="INFO" additivity="false">
        <appender-ref ref="CONVERSATION_FILE"/>
    </logger>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
